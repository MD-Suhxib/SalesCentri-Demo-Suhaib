import type { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Contact Intelligence Platforms',
};

export default function ContactIntelligenceLayout({ children }: { children: React.ReactNode }) {
	return children;
}


